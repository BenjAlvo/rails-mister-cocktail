<div class="banner" style="padding:15px; text-align:center;">
  <div class="banner-content">
    <h1>Cocktail</h1>
    <p>you choose the drink, we make sure you die</p>
    <%= link_to 'Create new cocktail', new_cocktail_path %></td>
  </div>
</div>
<div class="container">
  <div class="row">
    <% @cocktails.each do |cocktail| %>
    <div class="col-12 col-md-6 col-lg-4">
      <div class="card-trip">
        <%= cl_image_tag cocktail.photo %>
        <div class="card-trip-infos">
          <div>
            <h2><%=link_to cocktail.name, cocktail %></h2>
            <p>Try it!</p>
            <p><%= link_to 'Destroy', cocktail, method: :delete, data: { confirm: 'Are you sure?' } %>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>
